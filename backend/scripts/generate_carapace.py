"""DEPRECATED — DO NOT RE-RUN.

THE CARAPACE has a STRICT LOCKED image set per user spec. New angles are
NOT to be auto-generated, regenerated, or composed. This script is kept
only for historical reference and is intentionally disabled.

If a regeneration is ever required, confirm with the user first.
"""
import sys
print("Carapace generator is disabled. Image set is locked.", file=sys.stderr)
sys.exit(1)
