# PHILEON — Product Requirements Document

## Original Problem Statement
High-end luxury jewelry e-commerce site (PHILEON) with strict cinematic editorial UI (LA BÊTE visual language). Ongoing: content/UI expansion of Fine Jewelry, Inspiration Vault, and now Bracelets, with cinematic vertical galleries, autoplay-muted-loop hero video, and product-page detail pages per SKU.

## Product Registry (implemented)
- **Fine Jewelry Rings**: Rhythm Mesh, OVATION Ribbed Ring, BAJAN JOE Signet Ring, **QUADRIGA DOMINUS (NEW)**, RHYTHM MESH, LA BÊTE, CYPHER, BOUND, APEX, HOMAGE, TRIBUTE: LA BÊTE, BLESSED, COOGI I, TOLA II, Galatians 6:14, TRACE, IL MORSO DEL RE
- **Fine Jewelry Bracelets/Bangles**: **CRESTA NERA (NEW)** (10K/14K Yellow Gold · Black Diamonds · hinged)
- **Inspiration Vault**: Orbit Lumière, Viridian Teardrops, Prismatic Laurel, Noir Tide, Prima Wave, Noir Cadence, Liaison, STAMPEDE, NIGHTFANG, PARALLAX
- **Other**: PTP Cuff, Forme Cuff, Désir Corset, Rosaria, Alejandra Heels, La Marva, Annie Rose, Monika Couture, Katrina Cascata

## Architecture
- Frontend: React (CRA) — pages under `/app/frontend/src/pages`, product data in `/app/frontend/src/data/products.js`
- Backend: FastAPI (routes `/api/*`), MongoDB via `MONGO_URL`, Stripe checkout scaffolding in `/app/backend/routes/checkout.py`
- Shared components: `RingProductPage`, `RingSizeSelector`, **`WristSizeSelector` (NEW)**, **`BraceletSizeGuideModal` (NEW)**
- Custom IntersectionObserver lazy-loading + quadruple-redundant video loop (do not refactor)

## Changelog

### Feb 2026
- **[DONE Feb 21]** **GRAVITÉ — hero video wired (autoplay / muted / loop)** — Added merchant-supplied `7d7pvq85_XiaoYing_Video_1787276102537_HD.mp4` as the GRAVITÉ hero. `<video>` element rendered with `autoPlay muted loop playsInline preload="auto" disableRemotePlayback controls={false}` and an `onEnded` re-seek safety net. The approved black-background still (`G.hero` / `8y43t0ml`) is used as the poster so the frame is populated before the video decodes. When a shopper clicks any non-hero gallery thumb, the hero swaps to that still image; clicking thumb 0 restores the video. No audio (per merchant spec). Verified attributes and thumbnail-switch logic at 1440. No other product data touched — 10K default $5,995 CAD, 14K MOST CHOSEN $7,495 CAD, sizes US 5–10, CTA still disabled, backend trusted count still 84.
- **[DONE Feb 20]** **GRAVITÉ — final gallery wired with Facet Flow + heart-sphere macro** — Merchant supplied the three previously-missing URLs. Wired `2h0v5rth_1000171985.jpg` → `G.facetFront` (landscape Facet Flow front / collection); `tc7yzvzw_1000171990.png` → `G.facetOpp` (square Facet Flow opposite 3/4 editorial); `xufgzck8_1000171987.jpg` → `G.heartMacro` (dedicated ~6 mm rose-gold sphere + pavé macro). Restored the split-media layout for the Heart in the Pull section using the real macro (no more full-hero crop pretending to be a macro). Gallery now renders **5 unique thumbs** — no duplicates — at 390 / 768 / 1440. Data still preserved: 10K default $5,995 CAD; 14K MOST CHOSEN $7,495 CAD; US 5–10 half-steps with US 7 default; price flat across sizes; CTA disabled. Backend trusted count untouched.
- **[DONE Feb 20]** **GRAVITÉ — final cleanup pass** — Changed default metal from 14K → **10K ROSE GOLD** (`useState("10k-rose")`), so the initial displayed price on load is `$5,995 CAD`. 14K retains the `MOST CHOSEN` badge but is no longer auto-selected. Rewrote gallery: removed the duplicate slot-4 hero re-use, dropped the "later generated" black-background 3/4 alternate angles (`azx2ln7g`, `cfef8i4i`) that had drifted from the approved geometry, and swapped the Ladies → Rings card back to the single approved hero (`8y43t0ml`). Removed the placeholder "pretending-to-be-macro" image from the Heart in the Pull section — now text-only until a real ~6 mm sphere macro is supplied. New `G` map exposes `hero`, `facetSide`, and three `null` placeholders (`facetFront`, `facetOpp`, `heartMacro`) awaiting merchant-supplied URLs; the gallery array filters out `null` entries so only real approved images render. No motion effects were ever added. Backend trusted product count verified still **84**. Verified at 390 / 768 / 1440.
- **[DONE Feb 20]** **GRAVITÉ — Product #4 of 6 shipped as PRE-CAD placeholder** — New PHILEON Fine Jewelry ladies ring. Files created: `/app/frontend/src/pages/GravitePage.jsx`. Files modified: `/app/frontend/src/App.js` (canonical `/products/gravite` + secondary routes `/gravite`, `/fine-jewelry/gravite`, `/ladies/rings/gravite`); `/app/frontend/src/data/products.js` (canonical catalog card with `category: ['rings']`, `audience: ['ladies','collective','fine-jewelry']`, `status: 'placeholder'`, `purchasable: false`, `categoryOverrides` swapping the card image on Ladies → Rings); `/app/frontend/src/pages/ShopDropPage.jsx` (added `gravite` to `SHOP_COLLECTION_MAP` as `collective`). Two metal expressions (10K Rose Gold $5,995 CAD FOUNDATION; 14K Rose Gold $7,495 CAD SIGNATURE with `MOST CHOSEN` badge as default); US 5–10 in 0.5 steps with US 7 default; price flat across ring sizes; disabled CTA `CAD FINALIZATION IN PROGRESS`; inline SIZE GUIDE modal + full chart section; THREE STONES. ONE PULL. section with locked peridot dimensions (12.00 × 8.00 mm / 11.00 × 7.00 mm / 11.00 × 7.00 mm); The Heart in the Pull section (~6 mm rose-gold sphere with shallow heart engravings); Mixed Pavé Shoulders section (approx. 70 mixed green sapphire + white diamond melee, 1.2–1.4 mm); Specifications table with FINISHED WEIGHT: TBD after CAD; SEO title + meta. Placeholder guard in `ShopDropPage.jsx` prevents grayscale desaturation. Verified visually + DOM at 390 / 768 / 1440: hero=8y43t0ml (strongest 3/4), Ladies → Rings card=azx2ln7g (wider front/collection view), Fine Jewelry + Collective cards=8y43t0ml. Backend trusted product count confirmed still **84** — GRAVITÉ deliberately not in trusted catalog.
- **[DONE Feb 17]** **ROUGE SIREN Ladies category imagery — FIX #2 (visual, not just DOM)** — Root cause of the prior "still-wrong-images" report: the first pass mapped the Earrings card to a lifestyle model shot (`dfdtcf05_1000171241.png` — side-profile of model wearing earring) and Anklets to another model shot (`z2bfdgj2_1000171222.png` — model wearing full set) where the earrings were most prominent. Both looked like lifestyle/earrings imagery on-card, not product-focused / anklet-focused as required. Additional secondary root cause: `ShopDropPage.jsx` was flagging any card with `inventory_count === 0` as sold-out and applying `filter: grayscale(1)`, which stripped the rose-gold color from the placeholder ROUGE SIREN card. Fixes: (1) Earrings now maps to `d53p1y31_1000171117.png` — isolated ROUGE SIREN earring PRODUCT MACRO on black obsidian (no model, no lifestyle). (2) Anklets now maps to a new locally-cropped asset `/media/rouge-siren-anklet-crop.png` (Python/Pillow crop of the master composite `t4zwoen5_1000171127.png` — cropped from y=720 downward to fully remove the earring portion). Result: an isolated ROUGE SIREN ANKLET on obsidian with zero earring imagery. Sets remains on the full composite; Pendants remains on the pendant-on-chain macro. (3) Added an `isPlaceholder` guard in the renderer so `product.status === 'placeholder'` products skip the sold-out grayscale filter and render in their true rose-gold colorway. Verified visually at 390 / 768 / 1440 — Earrings and Anklets both now show product-focused rose-gold imagery, no lifestyle contamination.
- **[DONE Feb 17]** **ROUGE SIREN — Ladies category mapping, per-expression card imagery, and nav expansion** — Fixed miscategorized card art and added missing Ladies nav entries. Introduced `categoryOverrides` on the ROUGE SIREN catalog card (`/app/frontend/src/data/products.js`) mapping every Ladies category filter to the correct expression: `earrings` → side-profile earring worn photo (`dfdtcf05_1000171241.png`) · EARRINGS · $2,995 CAD; `anklets` → on-body full-set including anklet (`z2bfdgj2_1000171222.png`) · EARRING + ANKLET SET · $5,995 CAD; `sets` → complete set composite (`t4zwoen5_1000171127.png`) · EARRING + ANKLET SET · $5,995 CAD; `pendants` → pendant-on-chain macro (`y96fmfub_1000171223.png`) · PENDANT · $2,195 CAD. Changed `category` to an array so ROUGE SIREN surfaces in all four Ladies filters with a single canonical product record (all cards still link to `/products/rouge-siren`). `ShopDropPage.jsx` renderer now spreads `product.categoryOverrides[categoryParam]` before selecting `cardImage` and now renders `product.subtitle` (uppercase, tracked) in place of the material line. Added Ladies → **Sets** and Ladies → **Anklets** to `Header.jsx` (desktop + mobile) and `PhileonMenu.jsx`. Registered ROUGE SIREN in `SHOP_COLLECTION_MAP` as `collective` so it also appears in the Collective chip with the complete-set image. Verified at 390 / 768 / 1440 across all four categories via Playwright DOM assertions.
- **[DONE Feb 17]** **ROUGE SIREN hero — MP4 → animated WebP swap** — Replaced the `<video>` element in `/app/frontend/src/pages/VolutaPage.jsx` with `<img src="/media/rouge-siren-glitch-hero-loop.webp">`. Removed the `ROUGE_SIREN_VIDEO` MP4 constant and all video-only attributes (`autoPlay`, `muted`, `loop`, `playsInline`, `preload`, `disableRemotePlayback`, `poster`, `onEnded` re-seek handler). Kept the exact full-bleed hero sizing/classes (`voluta-hero-video-wrap` / `voluta-hero-video-el`) and the mid-page static on-body image unchanged. Verified in preview: `HERO_TAG=IMG`, `NATURAL_W=1920`, `COMPLETE=True`. Fixes real-device looping (WebP loops natively — no JS hacks needed).
- **[DONE Feb 11]** **RETRO BRED → private Drew's Vault release (Feb 2026 update)** — Repositioned from public Homage Series to a private, unlock-gated CAD collectible inside Drew's Vault. Three trusted CAD tiers wired into `FIXED_PRODUCTS`: Foundation $5,995 (RB-FND-925 · Sterling + synthetic) · Signature $10,495 (RB-SIG-10K · 10K + lab-grown) · Heirloom $11,795 (RB-HRL-14K · 14K + lab-grown). Pendant-only pricing; chain sold separately. Specs updated to merchant-signed values: 45×18 mm, 4.5 mm thick, ~12.5 g, 120 black + 150 white + 23 rubies = **293** total stones. Frontend `/drews-vault/retro-bred` (only route — no `/retro-bred` or `/products/retro-bred` public aliases) with `noindex,nofollow`, access gate that redirects unlocked visitors to `/secret-drop`. Every "Jordan", "Nike", "Jumpman", "Air Jordan" reference removed from page copy, alt text, marquee, and metadata. **83 products** trusted. **526 backend tests passing.**
- **[DONE Feb 11]** **Full pre-deployment checkout-readiness audit** — 82 products.
- **[DONE Feb 11]** **CRESTA NERA Hinged Bangle unblocked (wave 5)** — Merchant-signed 2 metals × 4 wrist sizes trusted matrix.
- **[DONE Feb 11]** **THE GRAND DAME unblocked (wave 4)** — Storefront's `GrandDamePage.jsx` + `products.theGrandDame` publish final trusted prices: Foundation $9,500 / Signature $12,500 / Heirloom $17,000 (parity across rose + yellow gold, 6 SKU variants). Migrated as `the-grand-dame` in `FIXED_PRODUCTS`.
- **[DONE Feb 11]** **Full-catalog migration wave 3** — 22 additional products migrated: 14 Inspiration Vault fixed-USD pieces (Altar, Caged Wings, Driven, Échelle, Lucent, Monaco, Nova, Oriel, Parabola Atelier, Parallax Drop Earrings, Ribbon Regale, Roseline, Stampede Set, Nightfang Set) + 8 previously blocked products confirmed as currently sold (Drew Face, La Madonna, La Scarpa Della Regina, Midweek, BAPE, Lisa, The Carapace, Neighborhood Nip). **80 products** trusted after wave 3. CRESTA NERA remains blocked pending merchant pricing.
- **[DONE Feb 11]** **Full-catalog migration wave 2** — 47 additional bespoke products routed through trusted server-side catalog via reusable `services/pricing_engine_catalog.py` resolver.
- **[DONE Feb 11]** **Live-pricing engine wired to trusted checkout** — 7 dynamic CAD rings (LA MARVA, ANNIE ROSE, RHYTHM MESH, TOLA II, PARABOLA, PARABOLA HERITAGE, OVATION) resolvable via `services/catalog._resolve_dynamic_ring`. Server pulls `metal_spot.get_spot()`, enforces `is_checkout_safe()` (fresh ≤10m / stale-usable ≤30m), computes CAD cents via the shared gold/silver delta formula + `round_luxury`. Fallback / expired snapshots → `LIVE_PRICE_UNAVAILABLE` 503.
- **[DONE Feb 11]** **PRICE_MOVED re-quote contract** — backend returns HTTP 409 when trusted price exceeds `MAX($50, 1%)` of the client's displayed snapshot. Redaction-safe payload: `product_slug`, `variant`, `old_display_price_cents`, `new_trusted_price_cents`, `currency`. `price_move_acknowledged=true` triggers a fresh market re-check; a second movement returns 409 again — no race-condition bypass. Frontend `Checkout.jsx` renders a cinematic PHILEON overlay.
- **[DONE Feb 11]** `MarketPricingContext` refreshes every 10 min (was 15), on `visibilitychange` after >10 min hidden, and on `phileon:refresh-market` event fired by `Checkout.jsx` at mount.
- **[DONE]** QUADRIGA DOMINUS — new gents statement ring with 4 colorway switch cards (Red/Black default, Black/Red, Green/Black, Black/Green), 10K/14K metal selector, gents US 7–15 half sizes, dynamic price matrix ($10,495–$13,750), shared `RingSizeSelector` reuse, size-guide CTA, substantial-band fit note. Registered in `/quadriga-dominus` (+ `/products/`, `/fine-jewelry/`) and appears in Gentleman's Club → Rings + Collective (excluded from Ladies and Inspiration Vault).
- **[DONE]** BAJAN JOE gallery — presentation-box (red PHILEON box) added as 9th/final gallery image; existing order preserved
- **[DONE]** CRESTA NERA Hinged Bangle — new product with metal (10K/14K YG) + wrist size (S/M/L/XL) required selectors, dynamic pricing matrix ($10,495–$12,595), bespoke page, hero video, main-page carousel placement, `WristSizeSelector` + `BraceletSizeGuideModal` shared components
- BAJAN JOE Signet Ring — additional gallery images (bar-hand editorial); reptile-detail zoom overlay; 17-size gents range 7–15 with required validation
- Ring-size selector dropdown positioning bug fixed (now anchors to button, not to parent root)
- OVATION Ribbed Ring — audience-conditional galleries (gents lifestyle + ladies palm shots), 8-image gallery + reorder

## Roadmap / Backlog

### P0 — Blocked on user env
- Provide `STRIPE_SECRET_KEY` + `STRIPE_WEBHOOK_SECRET` → unblock E2E checkout for all 11 catalog products
- Provide `METALS_API_KEY` → replace deterministic fallback in `metal_spot.py` with live provider quotes

### P1
- Migrate remaining 60+ bespoke products into trusted catalog (needs merchant pricing CSV)
- Analytics event when PRICE_MOVED is triggered (product, delta $, session id)

### P2
- Phase 2 Stripe Financing (Affirm / Klarna / Afterpay)
- Phase 3 PayPal integration
- Backend seed for CRESTA NERA if the `/api/products?featured=true` featured section is re-enabled on the homepage

### P3
- Abandoned-cart email logic
- Companion gents signet piece to BAJAN JOE
- Sticky mobile Add-to-Cart bar

## Changelog Highlights (Feb 2026)
- **Drew's Vault RETRO BRED visibility fix (Feb 2026)** — `DrewsVaultPage.jsx` restructured from a small 3-col card grid into a full editorial hero feature (large obsidian-framed hero image, red radial background, DREW'S VAULT · PRIVATE RELEASE eyebrow, RETRO BRED wordmark, campaign line "THE SOLE THAT BUILT A RELIGION, RECAST IN STONE.", secondary "Rubber wears down. Stone doesn't.", THE MARK block, ENTER RETRO BRED CTA to `/drews-vault/retro-bred`). Unlock state is now tracked in React state with focus/storage listeners so refresh persistence works. `RetroBredPage.jsx` gained an explicit **THE MARK** section with the approved editorial copy and a macro of the ruby-set central silhouette. No third-party brand terminology anywhere. Backend `pricing_engine_catalog.py` `retro-bred` fixed prices (5995/10495/11795 CAD), SKUs (RB-FND-925 / RB-SIG-10K / RB-HRL-14K), specs, and checkout untouched. Verified visibly rendered at 390 / 768 / 1440 with data-testid probes; refresh persists; locked visitors redirect to `/secret-drop`.

## Strict Rules
- Do NOT modify Stripe, `checkout.py`, `catalog.py`, `stripe_routes.py`, webhooks, or payment env vars
- Do NOT modify the SCACCO MATTO pilot catalog
- Do NOT refactor the gallery video loop logic

## Key File References
- `/app/frontend/src/pages/CrestaNeraBanglePage.jsx` — new bangle product page
- `/app/frontend/src/components/WristSizeSelector.jsx` — new shared wrist selector
- `/app/frontend/src/components/BraceletSizeGuideModal.jsx` — new shared bracelet size guide
- `/app/frontend/src/pages/BajanJoeSignetRingPage.jsx`, `/app/frontend/src/pages/OvationRibbedRingPage.jsx`
- `/app/frontend/src/data/products.js` — central product registry
- `/app/frontend/src/components/RingProductPage.jsx`, `/app/frontend/src/components/RingSizeSelector.jsx`
- `/app/frontend/src/components/PhileonCarousel.jsx`, `/app/frontend/src/pages/HomePage.jsx` (stripItems)
- `/app/backend/routes/checkout.py` — Stripe (on hold)
